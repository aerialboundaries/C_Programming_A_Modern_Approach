// 2026-06-09

/*
Explain what effect the following macro has on its arguments. You may assume
that the arguments have the same type. #define M(x,y)
((x)^=(y),(y)^=(x),(x)^=(y))

このマクロは、2つの変数 x と y
の値を一時変数（追加のメモリ）を使わずにそっくり入れ替える（スワップする）という効果を持っています。

このトリッキーなテクニックは、低レイヤの世界では「XOR交換アルゴリズム（XOR swap
algorithm）」として非常に有名です。

カンマ演算子（,）で区切られた3つのステップを順番に実行していくことで、なぜ値が入れ替わるのか、その仕組みをビットの動きと一緒に詳しく解説しますね。

具体例で見るステップごとの変化
理解しやすくするために、同じ型を持つ2つの変数 x と y
に、それぞれ以下の初期値が入っていると仮定します。

x ＝ A (初期値)

y ＝ B (初期値)

マクロの中身は (ステップ1, ステップ2, ステップ3)
と展開され、左から順番に実行されます。

ステップ1: (x) ^= (y)
x と y のXORを計算し、結果を新しい x に代入します。

x の新しい値 → A ^ B

y の値（不変） → B

ステップ2: (y) ^= (x)
y と、ステップ1で新しくなった x（値は A ^ B）のXORを計算し、結果を y
に代入します。
ここでXORの「同じ値を2回XORすると元に戻る（(B∧B)=0）」という性質が働きます。

y の新しい値 → B ^ (A ^ B) ＝ A * この時点で、y に元の x
の初期値（A）が引っ越してきました。

ステップ3: (x) ^= (y)
ステップ1の状態の x（値は A ^ B）と、ステップ2で新しくなった y（値は
A）のXORを計算し、結果を x に代入します。 ここでも同様に、A
が2回重なるため消去されます。

x の新しい値 → (A ^ B) ^ A ＝ B

これで、x に元の y の初期値（B）が引っ越してきました。

このマクロの注意点とベストプラクティス
このマクロは一見するとメモリを節約できるエレガントな裏技に見えますが、現代のC言語プログラミング（特に実務や安全なシステム開発）においては、使用を避けるべき「アンチパターン」とされています。理由は主に2つあります。

1. 同じ変数を渡すとゼロクリアされる（致命的なバグ）
もし、同じ変数へのポインタや、同じ配列の要素を指して M(a[i], a[j])
のように呼び出したとき、i と j が同じ値（同じメモリ空間）だと、最初のステップで
x ^= x が実行されてしまい、値が入れ替わるのではなく 0 になって消滅します。

2. 現代のコンパイラにとっては不要
昔のCPUのレジスタが非常に少なかった時代には有効なテクニックでしたが、現代の洗練されたコンパイラ（WSL上のGCCなど）は、通常のテンポラリ変数（一時変数）を使った以下のような標準的なスワップ処理を、CPUにとって最も高速な命令へ自動的に最適化してくれます。

// 現代のベストプラクティス（可読性が高く安全）
int temp = x;
x = y;
y = temp;
低レイヤのパズルや、パケット通信の暗号化のコードなどで見かけることがある面白いアルゴリズムですが、こういったトリッキーな挙動の裏にある「落とし穴」までセットで押さえておくと、より一層プロらしい視点が身につきますよ。

*/
