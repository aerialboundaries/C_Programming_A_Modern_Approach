/* C Programming A modern Approach ex4-14.c
 * Q: Supply parentheses to show how a C compiler would interpret each of the
 *    following expressions
 *
 *    (a) a * b -c * d + e
 *    (b) a / b % c /d
 *    (c) - a - b + c - + d
 *    (d) a * -b / c - d
 *
 * My Answers:
 *    (a) (a * b) -(c * d) + e
 *    (b) ((a / b) % c) / d
 *    (c) (- a - b + c) (-) + d
 *    (d) (a * -b / c) - d
 *
 * Correct Answers:
 *    (a) ((a * b) - (c * d)) + e
 *    (b) (((a / b) % c) /d)
 *    (c) (((- a) - b) + c) - (+ d)
 *    (d) (((a * (-b) / c) - d)
 *
 *  提示されたC言語の式に対する括弧付け（演算子の結合順序）の解釈は、ほとんど合っていますが、
 *  (c) と (d) の一部の解釈、特に単項演算子（unary
 * operators）の扱いに修正が必要です。
 *
 * 🔍 C言語の演算子の優先順位と結合規則
 * C言語のコンパイラが式を解釈する際、以下の規則に従います。
 *
 * 優先順位: 優先順位の高い演算子が先に評価されます（例：* や / は + や -
 * より優先）。
 *
 * 結合規則: 優先順位が同じ演算子については、結合規則に従って評価されます。
 *
 * ほとんどの二項演算子（*, /, +, -, % など）は左から右に結合します。
 *
 * 単項演算子（-, +, !, &, * など）や代入演算子は右から左に結合します。
 *
 * ✅ 正しい括弧付け
 * あなたの回答と正しい解釈を比較します。
 *
 * (a) a * b - c * d + e
 * 演算子	優先順位	結合性
 * *	高い	左から右
 * -, +	低い	左から右
 *
 * Google スプレッドシートにエクスポート
 * あなたの答え: (a * b) - (c * d) + e
 * 正しい解釈: ((a * b) - (c * d)) + e
 *
 * 解説: * が先に評価され、次に - と + が左から右に評価されます。あなたの答え (a
 * * b) -(c * d) + e は中間段階の解釈としては正しいですが、+ と -
 * の評価順序を示すために、さらに外側の括弧が必要です。
 *
 * (b) a / b % c / d
 * 演算子	優先順位	結合性
 * /, %	高い	左から右
 *
 * Google スプレッドシートにエクスポート
 * あなたの答え: ((a / b) % c) / d
 * 正しい解釈: (((a / b) % c) / d)
 *
 * 解説: / と %
 * は同じ優先順位で、左から右に評価されます。あなたの解釈は完全に正しいです。
 *
 * (c) - a - b + c - + d
 * 演算子	優先順位	結合性
 * 単項 -, 単項 +	高い	右から左
 * 二項 -, 二項 +	低い	左から右
 *
 * Google スプレッドシートにエクスポート
 * あなたの答え: (- a - b + c) (-) + d ❌
 * 正しい解釈: (((-a) - b) + c) - (+d)
 *
 * 解説:
 *
 * まず、単項演算子 (-a, +d) が評価されます。
 *
 * 次に、二項演算子 (-, +, -) が左から右に評価されます。
 *
 * あなたの答えにある (-) + d
 * のような括弧付けは、単項演算子の解釈として文法的に正しくありません。単項 - や
 * + は、後続する単一のオペランド (d) に作用します。
 *
 * (d) a * -b / c - d
 * 演算子	優先順位	結合性
 * 単項 -	最も高い	右から左
 * *, /	高い	左から右
 * -	低い	左から右
 *
 * Google スプレッドシートにエクスポート
 * あなたの答え: (a * -b / c) - d ❌
 * 正しい解釈: (((a * (-b)) / c) - d)
 *
 * 解説:
 *
 * まず、単項演算子 -b が評価されます（最も高い優先順位）。
 *
 * 次に、* と / が左から右に評価されます。a * (-b) が先に評価され、その結果が /
 * c されます。
 *
 * 最後に、二項演算子 - が評価されます。
 *
 * あなたの答え (a * -b / c) - d は、* と / のグループ分けがされていません。a *
 * -b の部分が先に評価されることを明確に示す必要があります。
 *
 * 修正版のまとめ
 * 式	あなたの答え	正しい解釈
 * (a) a * b - c * d + e	(a * b) - (c * d) + e	((a * b) - (c * d)) + e
 * (b) a / b % c / d	((a / b) % c) / d	(((a / b) % c) / d)
 * (c) - a - b + c - + d	(- a - b + c) (-) + d	(((-a) - b) + c) - (+d)
 * (d) a * -b / c - d	(a * -b / c) - d	(((a * (-b)) / c) - d)
 */
