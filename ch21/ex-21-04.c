/*
2026-06-11

The islower function, which belongs to <ctype.h>, tests whether a character is a
lower-case letter. Why would the following macro version of islower not be
legal, according to the C standard? (You may assume that the character set is
ASCII.)

#define islower(c) ((c) >= 'a' && (c) <= 'z')

My Answer:
結論からお伝えしますと、半分正解で、半分は「C言語の標準規格（C
Standard）」という観点から見落としがちな非常に重要な落とし穴があります。

あなたが答えてくださった「C言語はaからzが連続した番号（昇順）であることを保証していない」という理由は、実はEBCDICという古い文字コード体系を想定した場合には完璧な正解になります（EBCDICでは
'a' から 'z'
の間に別の文字が挟まっているため、このマクロは誤判定を起こします）。

しかし、今回の問題には「（文字コードが）ASCIIであると仮定してよい」という前提条件（You
may assume that the character set is ASCII.）が書かれています。ASCIIコード表では
'a'（97）から
'z'（122）までが完全に連続して綺麗に並んでいるため、値の範囲チェックという点だけを見れば、このマクロの挙動は「正しく動いてしまう」ことになります。

では、なぜASCIIだと仮定しても、このマクロはC言語の標準規格において不当（not be
legal）とされるのでしょうか。

その決定的な理由は、「引数 c
が複数回評価されてしまう（副作用の危険性がある）」からです。

詳しく解説しますね。

標準規格がマクロの「副作用」を禁止する理由
C言語の標準規格では、<ctype.h>
で提供される関数（およびそれを代替するマクロ）について、以下のような厳格なルールを定めています。

引数は「ちょうど1回だけ」評価されなければならない。

提示されたマクロの定義をもう一度見てみましょう。

C
#define islower(c) ((c) >= 'a' && (c) <= 'z')
この定義では、マクロの展開後に c
という文字が2回登場しています。これが大きな問題を引き起こします。

副作用が顕在化する具体例
もしプログラムの中で、インクリメント演算子（++）を使って、以下のように islower
を呼び出したらどうなるでしょうか。

C
 文字を読み進めながら小文字チェックをしたいとき
if (islower(*p++)) { ... }
これが本物の関数であれば、*p++
が評価されて「現在の文字」が関数に1度だけ渡され、ポインタ p
は1だけ進みます。これは意図通りの正しい挙動です。

しかし、上記のマクロが適用されると、プリプロセッサによって以下のように展開されてしまいます。

C
 プリプロセッサ展開後のコード
if (((*p++) >= 'a' && (*p++) <= 'z')) { ... }
お分かりいただけるでしょうか。*p++ が2回実行されてしまうのです。
その結果、最初の条件（>=
'a'）をチェックするときにポインタが1つ進み、次の条件（<=
'z'）をチェックするときには、すでに次の文字を指してしまっています。
さらにポインタは合計で2回も進んでしまいます。

これによってプログラムの動作は完全に破壊され、予測不可能なバグ（未定義動作）を引き起こします。

本物の標準ライブラリ（glibcなど）はどう解決しているか？
「じゃあ、マクロで高速化しつつ、引数を1回しか評価しないようにするにはどうすればいいの？」という疑問が湧きますよね。

実際の標準ライブラリ（<ctype.h>）では、配列のインデックス（目次）の仕組み（テーブル参照）を使って、引数
c が1回しか登場しないように巧妙にマクロが設計されています。

C
 実際の標準ライブラリのマクロイメージ
#define islower(c)  (__ctype_lookup_table[(int)(c)] & _ISLOWER_BIT)
これであれば、c はマクロの定義内に1回しか登場しないため、islower(*p++)
と書いてもポインタは正しく1回しか進みません。

回答のまとめ
あなたの回答（文字の連続性）:
EBCDICなどの文字コード体系を想定するならば大正解です。しかし「ASCIIと仮定する」という問題の縛りがあるため、今回は別の理由が求められています。

標準規格上の本当のNG理由:
マクロの定義内で引数 c を2回評価しているため。islower(*p++) や
islower(getchar())
のように副作用を伴う引数を渡したときに、意図しない複数回の実行（評価）が行われ、標準の
islower 関数と異なる不正な挙動を示してしまうためです。

教科書「C Programming: A Modern
Approach」のライブラリの章では、こうした「マクロの副作用と評価回数に関する安全性」が非常に厳しく問われます。マクロを作る時は「引数が何回展開されるか」を意識する癖をつけておくと、C言語のマスターにまた一歩近づきますよ。
*/
