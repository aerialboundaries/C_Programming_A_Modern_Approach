/*
2026-06-11

When a macro hides a function, which must come first in the header file: the
macro defini- tion or the function prototype? Justify your answer.

My Answer:
first prototype.

理由の解説（正当化）
C言語のビルドプロセスでは、コンパイラがコードを解析する前に、プリプロセッサ（Preprocessor）がマクロの置換を行います。この性質が順番に深く関係しています。

もし「マクロ定義」を先に書いた場合（誤り）
ヘッダファイルの中でマクロ定義を先に書いてしまうと、その直後に書かれているプロトタイプ宣言の関数名までプリプロセッサによって置換されてしまいます。

C
 1. マクロ定義を先に書く
#define getchar() getc(stdin)

 2. 関数プロトタイプを後に書く
int getchar(void);
この場合、2番のプロトタイプ宣言を処理するとき、プリプロセッサが getchar
という名前を見つけてマクロ展開してしまいます。その結果、コンパイラに渡るコードは以下のようになってしまいます。

C
 プリプロセッサ展開後のコンパイラが見るコード
int getc(stdin)(void);   構文エラー（Syntax Error）になります
getc(stdin) の後ろに (void)
が続くような奇妙なコードになり、コンパイラは意味を理解できずエラーを出してしまいます。

「プロトタイプ宣言」を先に書いた場合（正しい）
プロトタイプ宣言を先に書けば、その時点ではまだ getchar
のマクロ定義が存在しないため、正しく関数の宣言としてコンパイラに認識されます。

C
 1. 関数プロトタイプを先に書く（安全に宣言される）
int getchar(void);

 2. その後にマクロ定義を書く
#define getchar() getc(stdin)
これならプロトタイプ宣言が破壊されることはありません。そして、このヘッダをインクルードしたメインプログラム側では、それ以降に登場する
getchar() がすべてマクロとして安全に展開されるようになります。

21章の標準ライブラリでの実例
前に入手した <ctype.h>
などの実際の標準ヘッダファイルの内部を見ても、必ずこの順番で並んでいます。

C
 glibcの実際の並び順のイメージ

extern int isalpha (int);  まず本物の関数宣言

#define isalpha(c)   __isctype((c), _ISalpha)  その後に同名のマクロ定義
まとめ
正解: プロトタイプ宣言が先、マクロ定義が後。

理由:
マクロを先に定義してしまうと、後続のプロトタイプ宣言の関数名までマクロとして置換されてしまい、コンパイルエラー（構文エラー）を引き起こすため。

プリプロセッサが「上から順番に文字を置き換えていく」という性質をイメージすると、順番の重要性がすっきりと理解できると思います。
*/
